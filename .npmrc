# Use pnpm's default hoisting behavior (no shamefully-hoist needed)
# This removes the deprecation warning

# Auto-install peers (optional, for better compatibility)
auto-install-peers=true

# Strict peer dependencies (recommended)
strict-peer-dependencies=false

# GitHub Packages registry for @rev9dev-netizen scope
@rev9dev-netizen:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken={NPM_TOKEN}
